# Local setup

## To setup env variables

`firebase functions:config:get > .runtimeconfig.json`

## Setup Credentials
`export GOOGLE_APPLICATION_CREDENTIALS="path/to/key.json"`

> NOTE: Set fully qualified path
